{
  "name": "charmbooking",
  "private": true,
  "version": "0.0.1",
  "workspaces": [
    "libs/*",
    "api_gateway",
    "booking",
    "user"
  ],
  "scripts": {
    "build:common": "tsc -b libs/common",
    "build": "pnpm -r run build:common && pnpm -r run build",
    "start": "pnpm --parallel -r run start:dev",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "devDependencies": {
    "typescript": "^5.1.0",
    "globals": "^16.0.0"
  }
}
